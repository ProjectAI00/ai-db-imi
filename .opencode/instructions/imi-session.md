# IMI Session Contract (OpenCode)

Before responding or making code changes, run:

```bash
imi context
```

Task management must use IMI:

- Start/track work with `imi task ...`
- Record completion with `imi complete ...`
- Record decisions with `imi decide ...`

When work is done, always write back through IMI so future sessions inherit context.
